import React, { Component } from 'react';
import Card from '../components/Card'
import { shallow } from 'enzyme';

describe('Card', () => {
	let wrapper;
	beforeEach(() => {

	});
	it('should', () => {

	})
})